apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: igait
  labels:
    app: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 4173
    protocol: TCP