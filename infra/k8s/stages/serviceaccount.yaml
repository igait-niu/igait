apiVersion: v1
kind: ServiceAccount
metadata:
  name: stage-worker
  namespace: igait
# No RoleBinding — stage workers don't talk to the Kubernetes API.
# Their only out-of-pod surface is Firebase RTDB + S3, both reached
# via env-var credentials from the `igait-secrets` Secret.
