apiVersion: v1
kind: Service
metadata:
  name: igait-frontend
  namespace: igait
  labels:
    app: igait-frontend
spec:
  type: ClusterIP
  selector:
    app: igait-frontend
  ports:
  - port: 80
    targetPort: 4173
    protocol: TCP